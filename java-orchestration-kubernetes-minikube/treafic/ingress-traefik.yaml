apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: foo
spec:
  rules:
    - host: hello.minikube
      http:
        paths:
          - path: /
            pathType: Exact
            backend:
              service:
                name:  hello-service
                port:
                  name: http